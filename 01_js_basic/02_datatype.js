"use strict"; // treat all code as newer version of code 

// alert(3+3 )  we are using nodejs, not browser it will  show the error

// Ecma script is standard for JS

// all is the data type in JavaScript

// number => 2 to power 53
// bigint
// string ""
// boolean => true/false
//null=> standalone value  (it is empty )
// undefined => value is not defined 
// symbol => used when we have to define unique
let age =23
console.table([typeof(age),typeof null, typeof undefined ]) 
console.log(age)
// object 
// typeof is used to know the type of variable

/*  type of null is obj
    type of undefined is undefined
*/
